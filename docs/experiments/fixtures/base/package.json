{
  "name": "devdigest-experiments",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "Tiny Fastify orders API used as the base repo for the DevDigest skills experiments.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
