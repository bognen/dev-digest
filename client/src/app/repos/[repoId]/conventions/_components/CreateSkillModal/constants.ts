import type { SkillType } from "@devdigest/shared";

export const MODAL_WIDTH = 760;
export const BODY_ROWS = 16;

/** Skill types offered in the modal (the draft defaults to `convention`). */
export const SKILL_TYPES: SkillType[] = ["convention", "rubric", "security", "custom"];
