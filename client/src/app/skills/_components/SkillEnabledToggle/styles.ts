import type { CSSProperties } from "react";

/** Co-located styles for SkillEnabledToggle. */
export const s = {
  blocked: { display: "inline-flex", opacity: 0.45, cursor: "not-allowed" } satisfies CSSProperties,
} as const;
