import { SkillsListView } from "./_components/SkillsListView";

/* Route: /skills (Skills list). Thin route entry — the view, its rows, styles,
   helpers and i18n are colocated under _components. */
export default function SkillsPage() {
  return <SkillsListView />;
}
